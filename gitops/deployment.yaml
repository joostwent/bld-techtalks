---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bld-identity-demo
  namespace: pod-id
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: bld-identity-demo
  template:
    metadata:
      labels:
        app: bld-identity-demo
    spec:
      containers:
        - name: ticks
          image: acreuwaksbld2202.azurecr.io/sample/ticks:1.0.0
          resources:
            requests:
              memory: "4Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      restartPolicy: Always
